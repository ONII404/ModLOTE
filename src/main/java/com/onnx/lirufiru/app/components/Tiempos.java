package com.onnx.lirufiru.app.components;

public class Tiempos {
    public enum TipoDemanda {
        CONSTANTE, // Para demanda constante
        VARIABLE // Para demanda variable
    }

    public enum TipoTiempo {
        DIARIO, // Costo diario
        ANUAL // Costo anual
    }
}
